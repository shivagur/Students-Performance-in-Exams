"""from setup.py where it finds the find_packages() function 
will check in which folders does the __init__ file is present and it consider whole folder as a package and try to build there.
And then we can import in other files in the same folder"""
